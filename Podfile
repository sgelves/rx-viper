# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'test tabela' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for test tabela
  pod 'Alamofire', '~> 4'
  pod 'Kingfisher', '~> 5.0'
  pod 'RxSwift', '~> 5'
  pod 'RxCocoa', '~> 5'
end

target 'tests' do
  pod 'RxBlocking', '~> 5'
  pod 'RxTest', '~> 5'
end
